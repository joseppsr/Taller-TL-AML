# Prompt para Codex VS Code - Taller B4-T1

Quiero que modifiques el notebook `Taller_B4_T1_punto1_custom_layer.ipynb` para mejorar la preparación del pipeline antes de pasar a la fase de FAIR loss.

## Objetivo de esta modificación

Implementar dos mejoras concretas:

1. Sustituir el uso de `validation_split=0.2` dentro de `model.fit()` por una validación explícita con tres subconjuntos separados:
   - train
   - validation
   - test

2. Convertir el diccionario `class_weight` a tipos nativos de Python (`int` y `float`) para evitar que aparezca con tipos `np.int64` y `np.float64`.

No quiero cambiar la arquitectura del modelo ni la lógica de la capa customizada `DebtBurdenLayer`. La capa customizada actual queda aprobada.

---

## Contexto del notebook

El notebook trabaja con el dataset `application_train.csv` de Home Credit Default Risk.

Se usan estas columnas principales:

```python
cols = [
    'TARGET', 'CODE_GENDER', 'AMT_INCOME_TOTAL', 'AMT_CREDIT',
    'AMT_ANNUITY', 'DAYS_BIRTH', 'EXT_SOURCE_1', 'EXT_SOURCE_2', 'EXT_SOURCE_3'
]
```

La variable objetivo es:

```python
y = df['TARGET']
```

La variable sensible es:

```python
s = df['CODE_GENDER']
```

La variable sensible `CODE_GENDER` NO debe entrar como input del modelo. Debe conservarse aparte para la futura fase de fairness.

El modelo actual usa:

- variables escaladas como entrada principal;
- una rama financiera en bruto con `AMT_INCOME_TOTAL`, `AMT_CREDIT`, `AMT_ANNUITY`;
- la capa customizada `DebtBurdenLayer`;
- concatenación de variables escaladas + ratios saturados;
- red densa binaria con salida sigmoid.

---

## Cambio 1: crear split explícito train / validation / test

Actualmente el notebook hace un primer `train_test_split` para train/test y luego usa:

```python
validation_split=0.2
```

dentro de `model.fit()`.

Quiero que lo sustituyas por un split explícito en dos pasos:

1. Separar primero test:

```python
X_trainval, X_test, y_trainval, y_test, s_trainval, s_test = train_test_split(
    X,
    y,
    s,
    test_size=0.20,
    random_state=RANDOM_STATE,
    stratify=y
)
```

2. Separar validation desde trainval:

```python
X_train, X_val, y_train, y_val, s_train, s_val = train_test_split(
    X_trainval,
    y_trainval,
    s_trainval,
    test_size=0.20,
    random_state=RANDOM_STATE,
    stratify=y_trainval
)
```

Así el reparto final aproximado será:

- train: 64 %
- validation: 16 %
- test: 20 %

Debes imprimir y comprobar:

- shapes de `X_train`, `X_val`, `X_test`;
- distribución de `TARGET` en train, validation y test;
- distribución de `CODE_GENDER` en train, validation y test.

Crea o actualiza una celda markdown explicando por qué hacemos validación explícita:

```markdown
## Split explícito train / validation / test

En esta versión sustituimos `validation_split` por un conjunto de validación explícito. Esto permite conservar también la variable sensible `CODE_GENDER` en validación (`s_val`), lo cual será necesario para la siguiente fase de la práctica: medir y penalizar la dependencia entre las predicciones del modelo y la variable sensible.

El conjunto de test se mantiene separado y no se usa durante el entrenamiento ni durante la selección de hiperparámetros.
```

---

## Cambio 2: adaptar imputación y escalado al nuevo split

Asegúrate de que la imputación y el escalado se ajustan SOLO con train.

La lógica correcta debe ser:

```python
imputer = SimpleImputer(strategy='median')
X_train_imputed = pd.DataFrame(
    imputer.fit_transform(X_train),
    columns=X_train.columns,
    index=X_train.index
)
X_val_imputed = pd.DataFrame(
    imputer.transform(X_val),
    columns=X_val.columns,
    index=X_val.index
)
X_test_imputed = pd.DataFrame(
    imputer.transform(X_test),
    columns=X_test.columns,
    index=X_test.index
)
```

El `StandardScaler` también debe ajustarse solo con train:

```python
scaler = StandardScaler()
X_train_scaled = pd.DataFrame(
    scaler.fit_transform(X_train_imputed[scaled_features]),
    columns=scaled_features,
    index=X_train_imputed.index
)
X_val_scaled = pd.DataFrame(
    scaler.transform(X_val_imputed[scaled_features]),
    columns=scaled_features,
    index=X_val_imputed.index
)
X_test_scaled = pd.DataFrame(
    scaler.transform(X_test_imputed[scaled_features]),
    columns=scaled_features,
    index=X_test_imputed.index
)
```

Mantén la rama financiera en bruto a partir de los datos imputados, NO escalados:

```python
financial_features = ['AMT_INCOME_TOTAL', 'AMT_CREDIT', 'AMT_ANNUITY']

X_train_financial_raw = X_train_imputed[financial_features].astype('float32')
X_val_financial_raw = X_val_imputed[financial_features].astype('float32')
X_test_financial_raw = X_test_imputed[financial_features].astype('float32')
```

Y las variables escaladas:

```python
X_train_scaled_np = X_train_scaled.astype('float32')
X_val_scaled_np = X_val_scaled.astype('float32')
X_test_scaled_np = X_test_scaled.astype('float32')
```

Asegúrate de que `y_train`, `y_val` y `y_test` estén en formato compatible:

```python
y_train_np = y_train.astype('float32').values
 y_val_np = y_val.astype('float32').values
 y_test_np = y_test.astype('float32').values
```

Corrige cualquier espacio accidental si copias el bloque anterior.

---

## Cambio 3: modificar `model.fit()`

El entrenamiento debe usar `validation_data`, no `validation_split`.

Sustituye:

```python
validation_split=0.2
```

por:

```python
validation_data=(
    {
        'scaled_features': X_val_scaled_np,
        'financial_raw': X_val_financial_raw
    },
    y_val_np
)
```

El `fit()` debe quedar conceptualmente así:

```python
history = model.fit(
    {
        'scaled_features': X_train_scaled_np,
        'financial_raw': X_train_financial_raw
    },
    y_train_np,
    validation_data=(
        {
            'scaled_features': X_val_scaled_np,
            'financial_raw': X_val_financial_raw
        },
        y_val_np
    ),
    epochs=EPOCHS,
    batch_size=BATCH_SIZE,
    callbacks=callbacks,
    class_weight=class_weight,
    verbose=1
)
```

No uses `validation_split` en ningún punto del entrenamiento.

---

## Cambio 4: convertir `class_weight` a tipos Python nativos

Localiza la celda donde se calcula `class_weight`.

Sustituye el diccionario actual por una versión con tipos nativos:

```python
classes = np.unique(y_train)
class_weights_values = compute_class_weight(
    class_weight='balanced',
    classes=classes,
    y=y_train
)

class_weight = {
    int(cls): float(weight)
    for cls, weight in zip(classes, class_weights_values)
}

print('Class weights:', class_weight)
```

El resultado debe verse como:

```python
{0: 0.54..., 1: 6.19...}
```

No debe verse como:

```python
{np.int64(0): np.float64(...), np.int64(1): np.float64(...)}
```

---

## Cambio 5: adaptar evaluación y predicción al nuevo split

La evaluación final debe seguir haciéndose solo sobre test:

```python
test_loss, test_auc, test_accuracy = model.evaluate(
    {
        'scaled_features': X_test_scaled_np,
        'financial_raw': X_test_financial_raw
    },
    y_test_np,
    verbose=0
)
```

Las predicciones también deben usar test:

```python
y_test_proba = model.predict(
    {
        'scaled_features': X_test_scaled_np,
        'financial_raw': X_test_financial_raw
    }
).ravel()
```

Asegúrate de que todas las métricas finales se calculan sobre `y_test_np` y `y_test_proba`.

---

## Cambio 6: conservar `s_val` y `s_test`

No hace falta usar todavía `s_val` en este punto, pero debe quedar disponible en memoria para la siguiente fase de la práctica.

Añade una celda breve que confirme:

```python
print('s_train shape:', s_train.shape)
print('s_val shape:', s_val.shape)
print('s_test shape:', s_test.shape)
```

Y otra comprobación útil:

```python
print('Distribución de CODE_GENDER en validation:')
print(s_val.value_counts(normalize=True))
```

---

## Cambio 7: actualizar markdown de resultados

Actualiza la celda markdown final de conclusión para mencionar que ahora el notebook dispone de validación explícita.

Texto sugerido:

```markdown
## Conclusión actualizada del punto 1

Esta versión deja cerrada la arquitectura customizada del punto 1 y mejora el pipeline experimental mediante un split explícito en train, validation y test.

La validación explícita permite monitorizar el entrenamiento sin tocar el conjunto de test y, además, conserva la variable sensible `CODE_GENDER` en `s_val`, lo que será necesario para la fase posterior de FAIR loss y para la construcción de la curva de Pareto entre rendimiento predictivo y dependencia estadística.

La capa `DebtBurdenLayer` sigue siendo la aportación principal de este punto: calcula ratios financieros interpretables y los satura suavemente mediante `tanh` antes de concatenarlos con las variables escaladas y pasarlos a las capas densas.
```

---

## Restricciones importantes

- No cambies la arquitectura aprobada salvo que sea estrictamente necesario por nombres de variables.
- No elimines la capa `DebtBurdenLayer`.
- No metas `CODE_GENDER` como input del modelo.
- No ajustes imputador ni scaler con validation ni test.
- No uses `validation_split`.
- No cambies el objetivo: sigue siendo clasificación binaria de `TARGET`.
- Mantén las gráficas de curvas de loss y AUC.
- Mantén la tabla final de métricas.
- Mantén la matriz de confusión y el classification report.
- Asegúrate de que el notebook queda ejecutable de principio a fin sin errores.

---

## Resultado esperado

Al terminar, el notebook debe:

1. Ejecutarse completo sin errores.
2. Mostrar train, validation y test separados.
3. Conservar `s_train`, `s_val` y `s_test`.
4. Entrenar usando `validation_data`, no `validation_split`.
5. Calcular `class_weight` con tipos Python nativos.
6. Evaluar exclusivamente sobre test.
7. Mantener la arquitectura customizada con `DebtBurdenLayer`.
8. Dejar el notebook preparado para implementar la FAIR loss en el siguiente punto.
