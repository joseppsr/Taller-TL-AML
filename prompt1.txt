Actúa como un asistente experto en Python, Jupyter Notebook, TensorFlow/Keras y auditoría de modelos de clasificación aplicados a riesgo de crédito.

Contexto del proyecto
---------------------
Estoy trabajando en el taller "TALLER: B4-T1. Diseño de Redes Confiables (Justicia e Incertidumbre)".
El objetivo global de la práctica es diseñar, entrenar y auditar un modelo neuronal de clasificación para concesión de créditos, asegurando precisión, justicia/fair learning e incertidumbre.

Ahora estamos cerrando SOLO el punto 1 de la práctica:

1. Arquitectura Customizada:
   Implementar una capa customizada que calcule internamente ratios financieros de endeudamiento y aplique una saturación/restricción matemática antes de pasar la información a las capas densas.

Archivos disponibles
--------------------
- Notebook actual ejecutado: Taller_B4_T1_punto1_custom_layer.ipynb
- CSV de datos: application_train.csv
- Notebook base inicial: Lectura_datos_Taller_B4_T1.ipynb
- PDF de la práctica: Taller_B4_T1.pdf

El notebook actual ya funciona y no debe rehacerse desde cero. Quiero que lo mejores y documentes para que quede más sólido, auditable y preparado para enlazar después con FAIR loss, AutoML e incertidumbre.

Arquitectura aprobada
---------------------
Mantén la arquitectura inicial aprobada:

- Dataset: Home Credit Default Risk, usando application_train.csv.
- Variable objetivo: TARGET.
- Variable sensible: CODE_GENDER.
- CODE_GENDER debe conservarse para auditoría/fairness, pero NO debe entrar como variable predictora directa del modelo.
- Variables predictoras principales:
  - AMT_INCOME_TOTAL
  - AMT_CREDIT
  - AMT_ANNUITY
  - DAYS_BIRTH
  - EXT_SOURCE_1
  - EXT_SOURCE_2
  - EXT_SOURCE_3

La capa customizada debe seguir siendo una capa tipo DebtBurdenLayer que reciba variables financieras en bruto:

- AMT_INCOME_TOTAL
- AMT_CREDIT
- AMT_ANNUITY

y calcule internamente ratios como:

- annuity_income_ratio = AMT_ANNUITY / AMT_INCOME_TOTAL
- credit_income_ratio = AMT_CREDIT / AMT_INCOME_TOTAL

Después debe aplicar saturación suave con tanh:

- annuity_ratio_sat = tanh(annuity_income_ratio / tau_annuity)
- credit_ratio_sat = tanh(credit_income_ratio / tau_credit)

Usar valores razonables por defecto:

- tau_annuity = 0.5
- tau_credit = 5.0
- epsilon pequeño para evitar división por cero

Puntos técnicos que deben mantenerse
------------------------------------
1. No calcular ratios financieros sobre variables escaladas.
   Los ratios deben calcularse sobre importes financieros originales o imputados en escala monetaria real.

2. Mantener dos ramas de entrada:
   - Rama de variables escaladas para la red densa.
   - Rama de variables financieras en bruto para la capa customizada.

3. Imputar valores ausentes usando SOLO el conjunto de entrenamiento.
   No debe haber data leakage desde test.

4. Escalar variables usando StandardScaler ajustado SOLO con train.
   No debe haber data leakage desde test.

5. Mantener class_weight para tratar el fuerte desbalance de clases.

6. Mantener callbacks:
   - EarlyStopping
   - ReduceLROnPlateau

7. Mantener model.summary() para que pueda verse claramente la arquitectura y la capa customizada.

Mejoras que quiero que implementes
----------------------------------
A. Añadir una tabla final de métricas de test

Además de lo que ya tenga el notebook, añade una celda de código que calcule y muestre una tabla final con:

- test_auc
- test_accuracy
- balanced_accuracy
- recall_class_1
- precision_class_1
- f1_class_1
- tn
- fp
- fn
- tp

Usa sklearn.metrics.

La tabla debe mostrarse como DataFrame de pandas y, si procede, guardarse en CSV en una carpeta outputs/ con nombre:

outputs/metrics_punto1_custom_layer.csv

B. Añadir interpretación del desbalance de clases

Añade una celda markdown después de mostrar la distribución de TARGET explicando:

- que TARGET está fuertemente desbalanceada;
- que accuracy puede ser engañosa;
- que un modelo trivial que prediga siempre TARGET=0 tendría accuracy alta pero recall cero para la clase 1;
- que por eso son importantes AUC, recall clase 1, precision clase 1 y balanced accuracy.

Texto sugerido, puedes adaptarlo:

### Interpretación del desbalance de clases

La variable objetivo está fuertemente desbalanceada. Aproximadamente el 92 % de los clientes pertenecen a la clase `TARGET = 0` y solo alrededor del 8 % pertenecen a la clase `TARGET = 1`.

Esto implica que la métrica `accuracy` debe interpretarse con cautela. Un modelo trivial que predijera siempre “buen pagador” obtendría una accuracy muy alta, pero no detectaría ningún cliente con dificultades de pago.

Por este motivo, en este problema son especialmente relevantes métricas como AUC, recall de la clase minoritaria, precision de la clase minoritaria y balanced accuracy.

C. Añadir explicación de la capa customizada

Añade una celda markdown justo antes o justo después de definir DebtBurdenLayer explicando:

- qué ratios calcula;
- por qué se usan variables financieras en bruto;
- qué hace la saturación tanh;
- por qué la capa introduce conocimiento financiero e interpretabilidad;
- que la capa no tiene parámetros entrenables, sino que impone una transformación matemática.

Texto sugerido, puedes adaptarlo:

### Interpretación de la capa `DebtBurdenLayer`

La capa customizada introduce conocimiento financiero dentro de la arquitectura neuronal.

En lugar de dejar que la red aprenda desde cero todas las relaciones entre ingresos, crédito y anualidad, calculamos explícitamente dos ratios interpretables:

- cuota anual sobre ingresos;
- crédito total sobre ingresos.

Después aplicamos una saturación suave mediante `tanh`. Esta saturación evita que ratios extremos dominen el entrenamiento, pero conserva el orden económico de la señal: ratios más altos siguen generando valores más altos.

La capa no tiene parámetros entrenables. Su función es imponer una transformación financiera razonable antes de pasar la información a las capas densas.

D. Añadir interpretación de resultados de test

Después de la matriz de confusión, classification report y tabla de métricas, añade una celda markdown interpretativa.

Debe explicar:

- que la métrica principal de este primer modelo es AUC;
- que la accuracy no debe interpretarse sola;
- que class_weight aumenta el peso de la clase minoritaria;
- que esto suele aumentar recall de TARGET=1 pero también genera falsos positivos;
- que este modelo es una arquitectura base customizada, no un sistema final de concesión de crédito.

Texto sugerido, puedes adaptarlo:

### Interpretación de los resultados en test

El modelo obtiene una capacidad razonable para ordenar clientes por riesgo usando un subconjunto reducido de variables. En este contexto, la métrica AUC es más informativa que la accuracy, porque el problema está fuertemente desbalanceado.

La accuracy con umbral 0,5 no debe compararse directamente con la accuracy de un clasificador trivial, ya que el objetivo no es únicamente acertar la clase mayoritaria, sino detectar también clientes con dificultades de pago.

El uso de `class_weight` hace que la clase minoritaria tenga más peso durante el entrenamiento. Como consecuencia, el modelo puede detectar una proporción mayor de clientes con dificultades, aunque a costa de generar más falsos positivos.

Por tanto, este primer modelo debe entenderse como una arquitectura base customizada, no como un modelo final de decisión crediticia. Las siguientes fases deberán añadir auditoría de justicia, comparación con modelo base, ajuste de hiperparámetros e incertidumbre predictiva.

E. Añadir flags de missingness para EXT_SOURCE

El PDF de la práctica indica que las variables EXT_SOURCE_1, EXT_SOURCE_2 y EXT_SOURCE_3, con valores ausentes imputados, serán relevantes para estudiar incertidumbre.

Añade en el preprocesamiento tres columnas/arrays auxiliares de missingness:

- EXT_SOURCE_1_missing
- EXT_SOURCE_2_missing
- EXT_SOURCE_3_missing

Estas variables deben indicar 1 si el valor original estaba ausente y 0 si no lo estaba.

Para este punto 1 puedes dejarlas preparadas y documentadas, pero no es obligatorio incluirlas como entrada del modelo si eso complica la arquitectura actual. Preferencia:

- conservarlas en DataFrames auxiliares para usarlas después en incertidumbre;
- guardar un pequeño resumen del porcentaje de missingness por cada EXT_SOURCE;
- opcionalmente guardar ese resumen en:
  outputs/ext_source_missingness_summary.csv

Añade una celda markdown breve explicando que estos flags no se usan todavía en la arquitectura principal, pero quedan preparados para el análisis de incertidumbre.

F. Añadir control de reproducibilidad y outputs

Asegúrate de que el notebook:

- fija seeds de numpy, random y TensorFlow;
- crea carpeta outputs/ si no existe;
- guarda al menos:
  - metrics_punto1_custom_layer.csv
  - ext_source_missingness_summary.csv, si se implementa el resumen;
  - opcionalmente training_history_punto1_custom_layer.csv.

G. No romper el notebook

Muy importante:

- No rehagas el notebook completo desde cero.
- No cambies la arquitectura aprobada salvo ajustes mínimos necesarios.
- No añadas todavía FAIR loss.
- No añadas todavía Keras Tuner.
- No añadas todavía Monte Carlo Dropout ni incertidumbre predictiva.
- Este prompt es solo para cerrar y mejorar el punto 1: capa customizada.

Resultado esperado
------------------
Quiero que me devuelvas el notebook modificado, ejecutable de arriba abajo, sin errores, con:

1. Carga y preparación de datos.
2. Separación clara de TARGET y CODE_GENDER.
3. CODE_GENDER fuera de las variables predictoras.
4. Imputación y escalado sin data leakage.
5. Capa customizada DebtBurdenLayer documentada.
6. Modelo funcional con dos entradas.
7. Entrenamiento con class_weight y callbacks.
8. Curvas de entrenamiento.
9. Evaluación con AUC, accuracy, balanced accuracy, recall/precision/f1 de clase 1 y matriz de confusión.
10. Celdas markdown explicativas añadidas en los puntos clave.
11. Outputs guardados en carpeta outputs/.

Antes de terminar, revisa que todas las celdas se ejecutan en orden y que el notebook queda listo para continuar después con el punto 2: FAIR loss.
